/**
 * A chain a partner may send the payable coin on, in our own vocabulary rather
 * than the issuer's dashboard labels.
 */
export enum FundingNetwork {
  ETHEREUM = 'ETHEREUM',
  BSC = 'BSC',
  POLYGON = 'POLYGON',
  OPTIMISM = 'OPTIMISM',
  ARBITRUM = 'ARBITRUM',
  TRON = 'TRON',
}
